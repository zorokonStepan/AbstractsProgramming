```
    object.__setslice__(self, start, end, seq) - Позволяет задать поведение при попытке назначить срез для объекта-контейнера. Устарело в в Python 3.0
    
    object.__getslice__(self, start, end) - Позволяет задать поведение при попытке получить срез объекта-контейнера. Устарело в в Python 3.0
    
    object.__delslice__(self, start, end) - Позволяет задать поведение при попытке удалить срез из объекта-контейнера. Устарело в в Python 3.0
```