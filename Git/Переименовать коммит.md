```
    git commit --amend -m 'new_name_commit'  -  переименовать последний коммит
    git push origin your-branch-name -f
```

```
    git rebase -i HEAD~n
    git push --force-with-lease
```

```
    git rebase --abort
    отменить команду
```
