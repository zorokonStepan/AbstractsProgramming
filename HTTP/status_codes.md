```
    Всегда переадресуйте после POST-запроса (Гарри Персиваль TDD)
```

```
    302 - HTTP-переадресацию, которая имеет код состояния 302 и направляет браузер в новое место.(Гарри Персиваль TDD)
```