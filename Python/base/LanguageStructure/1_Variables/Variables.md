```
В Python нет переменных в классическом понимании.
В Python есть ссылки на объекты.
```
#### Присваивание
```
    1. a = 15
    2. b = c = d = [777, 888]            # у всех объектов один id // Каскадное присваивание.
    3. e, f, g = [1, 2, 3]               # e == 1, f == 2, g == 3  // Кортежное присваивание.
    4. x [+=, -=, *=, /=, //=, %=, **=]  # быстрее, чем x = x + 1  // Расширенные операторы присваивания.
```