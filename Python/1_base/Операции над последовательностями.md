```
    1. len() - возвращает длину последовательности, количество элементо или символов.
    2. Индексация.
    3. Срезы.
    4. Конкатенация - каждый раз создается новый объект, это дорогая операция. 
                      Правда для изменяемых типов данных, оператор +=
                      (например list a_list += b_list) добавит содержимое объекта из правой части
                      в объект в левой части, не создавая нового объекта.
    5. Мультипликация - аналогично конкатенации.                              
```