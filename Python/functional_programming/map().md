```
    >>> М = map(abs, (-1, 0, 1)) # mар возвращает итерируемый объект, а не список <mар object at 0х00000000029В75С0>
    >>> next(M) # Использование итератора вручную: результаты израсходуются
    1 # len () или индексация не поддерживается
    >>> next (М)
    О
    >>> next(M)
    1
    >>> next(M)
    Stopiteration
```